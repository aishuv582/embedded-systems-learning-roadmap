Day 109 of Electronics – 8085 Interrupts

Today, let's learn one of the most important topics in microprocessors: Interrupts in the 8085.

1. What is an Interrupt?

An interrupt is a signal that temporarily stops the execution of the current program so the microprocessor can execute a higher-priority task.

After completing the interrupt service routine (ISR), the processor resumes the original program.

Example:

You are reading a book.
Your phone rings.
You answer the call.
After the call, you continue reading from where you stopped.

The phone call is like an interrupt.

2. Why are Interrupts Needed?

Without interrupts:

CPU continuously checks every device (Polling).
CPU time is wasted.

With interrupts:

Devices notify the CPU only when they need service.
CPU works efficiently.
3. Interrupt Cycle
Main Program Running
        ↓
Interrupt Occurs
        ↓
Current Program Paused
        ↓
Save Program Counter
        ↓
Execute Interrupt Service Routine (ISR)
        ↓
RETI Instruction
        ↓
Resume Main Program
4. Types of Interrupts
Hardware Interrupts

Generated by external devices.

Examples:

Keyboard
Timer
ADC
Sensors
Software Interrupts

Generated by software instructions.

Examples:

RST 0
RST 1
RST 2
...
RST 7
5. Hardware Interrupts in 8085
Interrupt	Priority	Maskable	Vector Address	Trigger
TRAP	Highest	No	0024H	Edge + Level
RST 7.5	High	Yes	003CH	Edge Triggered
RST 6.5	Medium	Yes	0034H	Level Triggered
RST 5.5	Low	Yes	002CH	Level Triggered
INTR	Lowest	Yes	Non-vectored	Level Triggered

Priority Order

Highest

TRAP

↓

RST7.5

↓

RST6.5

↓

RST5.5

↓

INTR

Lowest
6. TRAP Interrupt

Features:

Highest priority
Non-maskable
Cannot be disabled
Used in emergency situations
Vector address = 0024H

Applications:

Power failure
Emergency shutdown
Critical fault handling
7. RST 7.5

Features:

Maskable
Edge-triggered
Stores the interrupt until serviced (latched)
Second highest priority

Vector Address:

003CH

Applications:

Fast external events
High-speed sensors
8. RST 6.5

Features:

Maskable
Level-triggered
Third priority

Vector Address:

0034H

Applications:

Peripheral requests
Communication devices
9. RST 5.5

Features:

Maskable
Level-triggered
Fourth priority

Vector Address:

002CH

Applications:

Low-priority peripherals
10. INTR (Interrupt Request)

Features:

Lowest priority
Maskable
Non-vectored
External device provides the ISR address during the interrupt acknowledge cycle.

Applications:

General-purpose interrupt requests
11. Maskable vs Non-Maskable Interrupts
Maskable	Non-Maskable
Can be disabled	Cannot be disabled
Lower priority	Highest priority
Used for normal events	Used for emergencies

Example:

RST 7.5 → Maskable
TRAP → Non-maskable
12. Vectored vs Non-Vectored Interrupts
Vectored Interrupt

The ISR address is predefined.

Examples:

TRAP
RST 7.5
RST 6.5
RST 5.5
Non-Vectored Interrupt

The ISR address is supplied externally.

Example:

INTR
13. Interrupt Enable/Disable Instructions
EI (Enable Interrupts)
EI

Enables all maskable interrupts.

DI (Disable Interrupts)
DI

Disables all maskable interrupts.

14. SIM Instruction (Set Interrupt Mask)

Used to:

Mask or unmask interrupts
Reset RST 7.5 latch
Control serial output
15. RIM Instruction (Read Interrupt Mask)

Used to:

Read interrupt mask status
Check pending interrupts
Read serial input data
16. Interrupt Service Routine (ISR)

An ISR is a special routine executed when an interrupt occurs.

Example:

Main Program

↓

Interrupt

↓

ISR Executes

↓

RETI

↓

Continue Main Program
17. RETI Instruction
RETI

Returns from the interrupt service routine and resumes execution of the interrupted program.

18. Polling vs Interrupts
Polling	Interrupt
CPU continuously checks devices	Device notifies CPU
CPU time wasted	Efficient CPU utilization
Slower response	Faster response
Simpler implementation	More efficient for multiple devices